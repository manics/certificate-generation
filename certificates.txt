http://www.wahlsten.com/openssl-ca

Certificate authority private key:
  openssl genrsa -des3 -out ca.key 1024

Certificate authority certificate:
  openssl req -new -x509 -key ca.key -out ca.crt -config openssl.cnf

Apache SSL certificate:
  openssl genrsa -out server-private.key 1024

Certificate request:
  openssl req -new -key server-private.key -out server.csr -config openssl.cnf

Sign certificate request:
  openssl x509 -req -in server.csr -CA ca.crt -CAkey ca.key -CAcreateserial -out server-public.crt


